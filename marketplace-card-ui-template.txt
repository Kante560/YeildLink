--- Marketplace Card UI Template (Reference for future implementation) ---

/*
This template preserves the card UI as shown in the reference image. Use this when your backend provides all required fields (price, rating, poster name, avatar, etc).
*/

<Card className="rounded-xl border shadow-sm hover:shadow-md transition">
  <CardContent className="p-5">
    <div className="flex items-center gap-3 mb-2">
      <div className="w-10 h-10 rounded-full bg-green-600 flex items-center justify-center text-white font-bold text-lg">
        {/* Crop initial */}
        C
      </div>
      <div className="flex-1">
        <div className="flex items-center gap-2">
          <span className="font-bold text-lg text-gray-900">Cassava</span>
          <span className="text-green-600 font-semibold text-base">N200/kg</span>
        </div>
        <div className="flex items-center gap-1 mt-1">
          <Star className="w-4 h-4 text-yellow-400 fill-yellow-400" />
          <span className="font-semibold text-gray-700 text-sm">4.5</span>
        </div>
      </div>
    </div>
    <div className="mb-1">
      <span className="font-semibold text-gray-700">Location:</span> <span className="text-gray-800">Kano, Nigeria</span>
    </div>
    <div className="mb-1">
      <span className="font-semibold text-gray-700">Quantity:</span> <span className="text-gray-800">2 tons</span>
    </div>
    <div className="text-gray-600 text-sm mb-2">High-quality cassava roots. Suitable for garri production.</div>
    <div className="flex items-center gap-2 mt-auto mb-3">
      <div className="w-7 h-7 rounded-full bg-gray-100 flex items-center justify-center font-bold text-gray-700">FO</div>
      <div className="flex flex-col">
        <span className="font-medium text-gray-800 text-sm">Fortune Okeke</span>
        <span className="text-xs text-gray-500 flex items-center gap-1"><Clock className="w-3 h-3" /> 1 week ago</span>
      </div>
    </div>
    <div className="flex gap-2 mt-auto">
      <Button className="bg-green-600 hover:bg-green-700 flex-1 rounded-[10px] py-2">Edit</Button>
      <Button variant="outline" className="flex-1 rounded-[10px]">
        <Trash2 size={16} />
      </Button>
    </div>
  </CardContent>
</Card>

// Fields needed from backend:
// - cropName
// - price
// - rating
// - location
// - quantity
// - description
// - posterName
// - posterAvatar (initials)
// - postedDate
// - (actions: Edit, Delete)

// When backend is ready, map these fields to the UI above.